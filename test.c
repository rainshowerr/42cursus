#include "push_swap.h"

/*
void	print_queue(t_deque *deque)
{
	t_node	*a;

	a = deque->top;
	while (a)
	{
		printf("%d ", a->data);
		a = a->next;
	}
}

int main()
{
    t_deque a;
    t_deque b;

    deque_init(&a);
    deque_init(&b);
    append_left(&a, 7);
    append_left(&a, 6);
    append_left(&a, 5);
    printf("\n");
    pb(&a, &b);
    pb(&a, &b);
    pb(&a, &b);
    print_queue(&b);
    // print_queue(&b);
    // printf("\n");

}
*/